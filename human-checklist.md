# Human Checklist

本文是已确认人类决策的台账：每条只记录**决策结论**，不复述方案正文。每组决策标明落地的 Owner 文档，细节以 Owner 为准。当 Owner 文档与本文冲突时，先在此确认修订，再改 Owner。

---

## 交付与流程

**Owner：** [`README.md`](README.md) Delivery sequence、[`AGENTS.md`](AGENTS.md)

- 首版完整方案已按人类访谈结果收敛，[`docs/flow_design.md`](docs/flow_design.md) 是跨页旅程的唯一 Owner。
- 人类需完整阅读并确认方案；未确认前不设计原型 Tasks，也不改造原型。
- 方案确认后才进入第二项交付：先按 Task Protocol 设计原型 Tasks，再由这些 Tasks 完善 UI 原型。
- 文档收敛原则：一个事实一个 Owner；本文只记决策，不建平行方案副本。

## 用户与首版范围

**Owner：** [`docs/flow_design.md`](docs/flow_design.md) §2、§3

- 核心用户是所有 A 股投资者，不要求预先已有关注方向。
- 首版完整闭环聚焦 A 股；港美股只作为外围行情、产业链或可比信息。
- 不做量化策略、回测、自动交易、明确买卖指令或仓位建议。
- 「关注 / 跟踪 / 持有」只表达用户与股票的关系，不增加成本、数量或仓位管理。
- 日 K、研究指标和事件按需获取，不持久化 tick；用户资料、证据、研究知识、Memory、配置和演化日志是持久化知识资产。
- 航运 / 中远海控与推理芯片 / 寒武纪分别作为周期型和成长产业链型的首批代表案例。

## 对外表述与术语

**Owner：** [`docs/product-plan/index.html`](docs/product-plan/index.html)（对外阅读稿，非事实源）

- 对外锚定表述（客户视角的核心诉求，原文）："当分析个股或行业时，我们想要：理解这个行业的发展趋势是什么；这些报告怎么得出来的结论，证据在哪里；当前整体行情还有哪些潜在的标的值得研究；这家公司当前估值是否合理——它是低估了还是高估了；如果我自己不会看财报、不懂专业分析方法，它可以帮我做；它还会帮我持续整理资料，追踪关键指标，并定期复盘、更新判断。"
- 对外材料以该段为统一开头，统一使用投资者术语：广度发现、主动分析、明确估值、证据可查、持续追踪。
- 产品对外称"投研助手"；对外阅读稿不使用"首版"字样，内部文档保留首版范围语义。
- 对外与对内的术语映射如下，两侧不得再各自造词：

| 对外用词 | 内部用词 | Owner |
|---|---|---|
| 健康评估 | 研究状态（绿 / 黄 / 红 / 灰） | `research-design/` |
| 研究档案 | 对象知识目录（Purpose + 证据层 / 知识层 / 判断层） | `knowledge-design/` |
| 研究依据 | Wiki 页面与证据锚点 | `knowledge-design/` |
| 当前报告 | `judgment/report.md` | `knowledge-design/`、`research-design/` |
| 基本面速览 | 基本面速览（内外一致） | `market-design/` |
| 变更记录 / 恢复 | 本地 Git checkpoint 与仓库级日志 | `knowledge-design/` |
| 分析方法 | 研究 Skills 与默认主线 | `research-design/`、`data-design/` |

**待人类处理的对外文案冲突：** 对外稿现有表述暗示系统在用户不使用时仍持续工作（「财报季、重大公告、每周复盘、季度回顾，它跟着你的节奏一直工作」）。架构已确认周节拍是纯前台形态，应用关闭时不运行。对外稿需要改成诚实说法（例如「每次打开时替你检查一遍，并告诉你上次检查是什么时候」）。对外稿由人类拥有，此处只登记冲突，不代改。

## 核心产品目标

**Owner：** [`docs/flow_design.md`](docs/flow_design.md) §1、§2

- 核心任务是判断一个行业或股票当前是否值得关注、估值是否有吸引力、为什么、什么条件会改变判断；资料整理、Wiki 和报告是手段，不是产品中心。
- 系统必须主动提供适用的分析框架，不要求用户每次先告诉 Agent 应关注什么。
- 核心分析链：行情 / 财务 / 行业数据 → 业务与行业机制 → 收入、成本、利润和现金流拆分 → 关键先行指标 → 估值模型与价值区间 → 当前价格、安全边际和估值吸引力 → 持续验证。
- 当前价格是资产定价判断的一半：实时价格相对最近完成的价值区间决定估值位置和安全边际；财务、行业趋势和先行指标决定价值区间是否需要更新。
- 产品可以明确判断偏高估、合理、偏低估或证据不足并解释原因，不写成买卖或仓位指令。
- FCISU、来源位置、数据时间和时效要求是所有判断的可信度底线。

## 行情、发现与基本面速览

**Owner：** [`docs/market-design/market_design.md`](docs/market-design/market_design.md)

- 用户有两种起始情况：从广度探索发现后加入，或已有股票直接加入；两者汇入同一研究流程，不构成两个产品入口。
- 个股详情页和行业详情页均提供「加入研究」，支持单个与批量；加入个股时可选标签，默认「关注」。
- 个股与行业详情是无自动生成结论的数据 Dashboard；公共助手仍可分析当前页面。
- **基本面速览由行情模块拥有**，研究对象页复用同一合同。它是完整分析的第一层输出，不替代估值与价值区间判断，也不改变对象页的既定信息架构。
- 速览必须回答：这家公司靠什么赚钱、主营与所属行业、各业务对收入和归母利润的贡献占比、利润由什么驱动、当前应重点观察哪些变量与先行指标，以及主要风险、知识缺口和数据时间。
- 速览由系统主动形成，不要求用户先准备财报、先指定分析方法或先说明应关注什么。
- 速览以结构化 Dashboard 呈现——占比用可视化与紧凑表格、观察点与风险用结构化卡片——不输出长文本段落；先给结论与占比，再给依据、来源与缺口。
- 速览是详情页上按需打开的一层，不是详情页数据体的一部分，因此详情页「无自动生成结论」的定位不被破坏。

## 研究对象与关注生命周期

**Owner：** [`docs/research-design/research_design.md`](docs/research-design/research_design.md)、[`docs/flow_design.md`](docs/flow_design.md) §6

- 首版研究对象只有个股和行业；证券经营知识直接沉淀在个股对象中，不预建独立公司实体或证券—公司映射。
- 加入个股时自动初始化其主行业对象，并建立可双向跳转、带证据的关系。
- 加入后立即出现占位卡，不等待后台初始化；占位卡不持续展示队列或处理术语。
- 初始化至少建立：对象身份、主行业关系、适用分析框架、少量有证据事实、知识缺口，以及允许稀疏的初始报告。
- 初始化失败自动重试三次；仍失败后暂停，用户再次点击即可恢复。
- 取消关注只移除列表关系并停止主动更新，不删除已形成的知识。
- 彻底删除从对象页「更多 → 删除研究档案」进入；用户上传的原始资料保留在「研究资料」中并解除关联。

## 研究信息架构

**Owner：** [`docs/research-design/research_design.md`](docs/research-design/research_design.md)

- 研究页只保留「行业 / 个股」两个二级 Tab，不设全局报告 Tab。
- 每个 Tab 包含对象列表和「最近更新」；个股卡展示关注标签、最近完成的研究状态、关键变化时间和待复核 / 未查看标记。
- 每个对象使用统一骨架：研究状态 → 实时指标 → 当前报告 → 研究依据 → 关系跳转 → 助手。
- 界面不强调 Wiki 技术概念；对象页使用「研究依据」等业务语言。
- 复核结果进入研究页「最近更新」，并复用已有的关注股票公告 / 新闻聚合；不新建通知中心，不做外部推送接口。

## 研究状态与复核节奏

**Owner：** [`docs/research-design/research_design.md`](docs/research-design/research_design.md) §5.1、§6

- 研究状态四态：绿＝条件总体有利，黄＝指标分化，红＝条件明显恶化，灰＝证据不足。
- 状态不是黑盒评分或买卖信号；每态可展开查看原因、支撑证据、证据强度、数据时间和改变判断的条件。
- 状态显示最近一次完成分析的结果，不随 tick 闪烁。
- **复核分轻、深两级：时间负责保证不漏，事件负责决定何时重算。**
- **轻复核按周节拍自动执行**，只检查指标越阈、价格相对价值区间的位置和未处理的新资料；只输出「查过了，没变」或标记待复核，不重算价值区间、不重写报告、不改变研究状态。
- **周节拍只在应用运行时生效。** 各对象从自身上次复核起独立计时，不设全局时点，因此不会集中复核；启动时对超过一周的对象补做一次，无论关闭多久都不回放历史节拍。这条解决了原先待定的「周节拍起点与分批」问题。
- **深复核由事件驱动**：定期报告实际摄取完成、重大公告或行业事件影响关键变量、指标越阈已标记待复核、Wiki 出现实质新事实或冲突解决、用户主动要求或采用持续假设、对象首次初始化。
- **定期报告以资料实际到位为准，不按日历季度。** 日历季度末本身不构成复核条件。
- **缺报兜底：** 超过法定披露截止日与宽限期仍未取到报告时，不执行深复核，而是标记财务证据过时、在「最近更新」说明缺哪一期、登记为知识缺口，必要时研究状态转灰。系统不因为到时间了就用旧输入空转一遍并宣称已复核。
- 每个对象只有一份持续更新的当前报告，不提供版本列表；更新失败时保留最后成功报告。
- 报告归属于具体对象，可导出，也可作为新资料重新摄取。

## 方法、Skills、估值与输出边界

**Owner：** [`docs/research-design/research_design.md`](docs/research-design/research_design.md) §7–§8、[`docs/research-design/analysis_frameworks.md`](docs/research-design/analysis_frameworks.md)

- 默认分析主线：业务 / 行业机制 → 收入、成本与利润拆分 → 财务模型 → 关键先行指标 → 用实时数据替换滞后信息 → 利润趋势与现金流 → 估值模型 → 当前价格与价值区间比较 → 证伪条件。
- 方法体系由默认主线和少量通用方法组成；不要求按行业预建固定 Skill 或必填字段清单。稳定方法可以后续沉淀，但不是首版分析前提。
- 首版通用方法参考用户既有 Second Brain 中实际使用过的内容，只作为方法案例，不作为预置行业模板或运行时依赖。
- 既有方法中的买卖点、仓位和阈值必须转换为可解释的研究条件、触发阈值和绿 / 黄 / 红方向状态；证据不足时用灰色。
- Skills 在「数据 → 配置 → 研究 Skills」中管理，底层优先使用 Pi Skills 和 Pi Package。
- 可以输出价值区间、安全边际和估值吸引力，但必须展示方法、关键假设、数据时间、证据和失效条件，不输出确定买卖或仓位指令。
- 估值情景由助手识别意图并调用结构化输入 Tool，用户确认后由确定性函数重算。
- 临时情景不改变当前报告；采用为持续假设需再次确认，写入 Memory 并触发复核。

## 架构与运行形态

**Owner：** [`docs/architecture/architecture.md`](docs/architecture/architecture.md)

- **允许 LLM 做判断，但只能用结构性手段约束它。** 产品承诺的输出是研究状态、价值区间和估值位置，都是判断而非资料整理，因此不采用「只编译事实、判断全交给人」的模式。约束落在 FCISU 分级、Raw 不可销毁、冲突不可静默解决、机械工作归确定性代码这四条上，而不是提示词。
- 第一原则是**机械归代码、判断归 LLM、裁决归人**。
- **存储分四层，权威唯一：** `raw/sources/` 证据层只追加；`wiki/` 知识层只承载事实与关系；`judgment/` 判断层存放会被推翻的结论；SQLite 是可丢弃重建的投影层，不承担任何权威。
- 三条越界规则：Wiki 页面不得写入研究状态、价值区间或估值结论；判断层复述 Wiki 事实必须带脚注锚点指回；删除 SQLite 后重新扫描知识仓库必须能完全重建。
- **知识写入分两阶段：** Agent 只产出变更集（慢、非确定、不落盘），确定性 Command 负责校验、写盘、追加日志、更新投影和单次 commit（快、确定）。校验不过则整个变更集丢弃，工作区从未被修改。
- **锁按对象加：** 同对象串行，跨对象并行；不引入乐观并发控制、影子目录或工作分支。
- **周节拍是前台形态：** 只在应用运行时工作，不驻留后台、不注册开机自启。各对象独立计时，启动时对超期对象补做一次，无论关闭多久都不回放历史节拍。

## 证据与知识生命周期

**Owner：** [`docs/knowledge-design/wiki.md`](docs/knowledge-design/wiki.md)

- 每个对象借鉴 llm-wiki 并按三层组织：`raw/sources/`（证据）、`wiki/`（事实与关系）、`judgment/`（判断）；`purpose.md` 在对象根。
- **判断层含三个文件：** `verdict.md`（结构化判断状态）、`report.md`（当前报告全文）、`memory.md`（长期假设与待验证问题）。Memory 属于判断层而非知识层，因为假设是判断的输入，不是已确立的事实。
- **`verdict.md` 上半可重写、下半只追加**：上半是当前判断、研究状态、最近完成的价值区间及假设快照、失效条件和待复核标记；下半是状态变更时间线，记录变化、原因、依据和数据时间，永不重写。这是产品承诺「改了会告诉你为什么改，以前的记录都在」的落点，与「不做报告版本列表」不冲突。
- **轻复核只能写 `verdict.md` 的待复核标记和 `wiki/log.md`**，触及其他内容的变更集必须被拒绝。
- **报告是判断层的一份文档，不是渲染时拼装的视图。** 它承载贯穿的论证行文，深复核整体重写，更新失败保留最后成功版本；报告落后于 Wiki 由「待复核」如实表达，不悄悄改写。
- Raw 摄取和纠错只追加、不原地改写；用户在「研究资料」中明确删除是唯一例外，必须经过影响预览、二次确认并保留可恢复 checkpoint。
- Wiki 由 Agent 按 Schema 创建、更新、重组和交叉引用；只有 `wiki/log.md`、Raw 和 `verdict.md` 的只追加区是不可重写的历史。
- **Schema 的作用域是仓库级**：`schema/stock.md` 与 `schema/industry.md` 两份全局 Schema，Agent 在任一对象上摸索出的稳定维度写回全局，一次学会处处生效；对象目录下的 `schema.md` 只在确需偏离时存在，只记偏离项。全局演化不回溯重写已有对象。
- 知识链路 `Purpose + Schema → Source / Raw → Agent 分析与 Evidence → Wiki → Log`；判断链路 `Wiki + 动态市场数据 + 模型假设 → 通用方法分析 → judgment/`。两者方向不可颠倒。
- **证据锚点固定为 Markdown 脚注**，数字与叙述型判断共用同一机制；整表同源时表格可额外带来源列，但不替代脚注。
- 每条锚点包含 FCISU、Source、原文位置、摘录、`data_as_of`、口径单位和失效条件。**时效状态不写入文件**，由数据时间与 Schema 时效窗口在渲染时推导。
- FCISU 固定为 Fact、Claim、Inference、Synthesis、Unattributed；用户陈述在没有外部证据前只是用户假设。
- 事实冲突不静默覆盖：保留双方、标记存疑、形成知识缺口，新证据到位后再收敛。
- 关系是一等知识结构，个股—行业及其他研究关系必须有证据。
- Memory 只保存长期有价值的内容，不保存完整聊天；每轮回答后异步提取，不逐轮确认。

## 数据、资料与恢复

**Owner：** [`docs/data-design/data_design.md`](docs/data-design/data_design.md)

- 数据页二级 Tab 固定为「数据源 / 研究资料 / 配置」。
- 「数据源」展示真实业务通道，不把 `Vibe Research`、`Daily Stock Analysis` 或 `llm-wiki` 作为用户可见数据源名称。
- **系统对已加入研究的对象主动摄取新资料**：行情按需读取，公告 / 新闻 / 研报按日增量，定期报告在披露窗口内提高检查频率。未加入的对象不建立后台任务。
- 数据模块拥有「什么时候去取、取什么、取失败怎么办」；资料到位后是否复核由研究模块决定。
- 「研究资料」只管理用户历史上传的文件、URL 和粘贴资料，不展示 Wiki。
- 对象页提供「添加资料」并默认绑定当前对象；数据页支持批量导入，歧义时请用户选择。
- 用户纠错不做无痕覆盖；编辑或删除先展示受影响对象、证据和当前报告，二次确认后形成原子事务。
- 「配置」使用卡片进入 Schema、研究 Skills、标签、时效规则和变更记录；Schema 默认折叠为高级能力，证据底线不可删除。
- 所有持久化知识文件进入同一本地 Git 仓库；SQLite、接口缓存和临时文件不纳入。SQLite 是可丢弃重建的投影层，删库后重新扫描知识仓库即可恢复列表、状态和时间。
- 首版不执行 push、不提供分支或版本号；用户看到「变更记录 / 恢复」，恢复本身生成新日志和 commit。
- 删除研究档案前必须用业务语言列出将删除和将保留的内容，特别说明「你上传的原始资料不会删除」。

## 助手与结构化动作

**Owner：** [`docs/flow_design.md`](docs/flow_design.md) §9

- 公共助手存在于所有页面，读取当前页面上下文；页面切换或刷新即开始空白对话，不维护全局或对象级会话，也不提供历史列表。
- 同一路由内改变筛选不清空对话，但新数据和筛选条件必须进入后续上下文。
- 个股研究页装载个股 Wiki、对象 Memory、关联行业 Wiki、启用 Skills 和页面快照；行业研究页装载行业 Wiki、Memory、通用 Skills 和页面快照；行情与数据页只装载页面类型、筛选、可见数据和时间戳。
- 大体量或最新数据由工具按需获取；助手可自行调整术语和解释深度，但关键判断仍需给出依据、缺口和改变判断的条件。
- 工具失败必须明确数据缺口，不得补写无来源结论。
- 查询、取数和临时计算可直接执行；用户发起的修改、删除和采用假设需要结构化确认；已启用的后台摄取、研究更新和 Memory 提取自动执行并记录日志，不逐次打扰。
- Agent 不直接模拟界面操作，也不绕过确认修改存储。

## 失败语义

**Owner：** [`docs/flow_design.md`](docs/flow_design.md) §10，各模块合同的失败语义表

- 单个实时接口失败只影响对应数据块，显示最后成功时间和过期状态。
- Wiki 或报告更新失败时保留最后成功结果并提供重试，不用空白覆盖。
- 助手工具失败必须明确不可用数据和影响，不得伪造结论。
- 所有用户确认后的知识修改必须原子；不能完整落盘、写日志并形成 checkpoint 时视为失败。

## 运行时与复用边界

**Owner：** [`README.md`](README.md) Capability boundaries

- 底层统一采用 Pi SDK + Extensions，不另建 Agent Runtime，也不依赖 Pi 内部模块。
- 优先复用 `AgentSession`、Extension lifecycle、Commands、Skills、Prompt Templates、Tools、Session Entries、`SessionManager` 和 Pi Package。
- 优先复用 `Vibe-Research` 的行情、估值、财务、公告、行业工具、Function Calling 和流式回答，不重复建设数据接入层。
- `llm_wiki` 提供 Source、Schema、摄取、关系、日志、检查和增量维护思路；Vibe-Finance 仍拥有自己的产品合同与界面。它的知识层可以直接借鉴，但它只编译事实、不做判断，因此判断层是 Vibe-Finance 自己的设计，不在其覆盖范围内。
- 检索不引入 embedding 或向量库：知识按对象天然分片，单对象几十页用 `wiki/index.md` 导航即可，跨对象定位由 SQLite 投影承担。
- `workbench-desktop-finance-desktop` 只用于参考 SQLite migration 和 Pi + Extensions 的组织方式。

## 原型权威与验收

**Owner：** [`docs/flow_design.md`](docs/flow_design.md) §5、§12

- `prototype/02-research.html` 是唯一正式研究原型；`06-research-v2`、`07-research-v3` 和 `08-research-v4` 不再作为候选。
- 原型阶段完善 `prototype/01–05` 及共享资源，使用可点击模拟数据和前端状态，不连接真实 Pi、接口或持久化层。
- 原型必须验证四条用户主线：广度发现与加入（含基本面速览、批量、占位、失败恢复、档案删除与恢复）；关注对象的当前状态与对象页阅读；资料进入、维护与恢复；结论、复核节奏与估值结构化确认。
- 原型验证产品方向，不反向决定产品范围。
